    {
      "publicKey": "{{peer_publickey}}",
      "endpoint": {
        "host": "{{peer_host}}",
        "port": {{peer_port}}
      },
      "metadata": {
        "name": "{{peer_name}}",
        "roles": "{{peer_role}}"
      }
    }