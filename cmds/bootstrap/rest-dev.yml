version: '3'

networks:
  default:
    external:
      name: bootstrap_default
services:
  rest-gateway:
    image: techbureau/catapult-rest-dev
    command: ash -c "cd /catapult-rest && ./yarn_setup.sh && cd rest && HASHING_FUNCTION=keccak yarn start:debug resources/rest.json"
    stop_signal: SIGINT
    ports:
    - "3000:3000"
    volumes:
    - ../../catapult-rest:/catapult-rest/
